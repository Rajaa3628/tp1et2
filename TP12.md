## **TP— 1 et 2** 

## Construction d’un Projet Scientifique Reproductible et Analyse de Stabilité Numérique 

## **Module :** Science des Données et Calcul Scientifique Avancé 

## D1— Session 2026 

## **Table des matières** 

|**1**|**Objectifs Pédagogiques**||**2**|
|---|---|---|---|
|**2**|**Partie 1 : Isolation et Gestion de l’Environnement**||**2**|
|**3**|**Partie 2 : Confguration et Modularité du Pipeline**||**3**|
|**4**|**Partie 3 : Défnition des Tests Unitaires**||**4**|
|**5**|**Partie 4 : Gestion du Versionnement Scientifque**||**4**|
|**6**|**Partie 5 : Intégration dans l’Orchestrateur Principal**||**5**|
|**7**|**Partie 6 : Stabilité Numérique**||**6**|
||7.1<br>Exercice 6.1 : Horizon de visibilité numérique et non-associativité .|. . . . . . . .|6|
||7.2<br>Exercice 6.2 : Le Multiplicateur de Mensonge — Sensibilité au conditionnement .||7|
||7.3<br>Exercice 6.3 : Inversion Explicite vs Factorisation Directe<br>. . . . .|. . . . . . . .|7|
|**8**|**Questions de Réfexion et Livrables**||**8**|
||8.1<br>Questions de Contrôle de Validation (À rédiger sur votre rapport)|. . . . . . . .|8|
||8.2<br>Livrables<br>. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .|. . . . . . . .|9|



1 

## **1 Objectifs Pédagogiques** 

Ce projet de TP se divise en deux volets essentiels de votre formation de chercheur : 

1. **Ingénierie de la Reproductibilité :** Structurer un projet Python, isoler son environnement d’exécution, externaliser sa configuration et automatiser ses tests. 

2. **Stabilité Numérique et Algèbre Linéaire :** Mettre en pratique les concepts clés du dernier cours (conditionnement, arithmétique IEEE 754, décompositions matricielles) pour prévenir l’effondrement d’un modèle face au bruit numérique. 

## **Objectif d’Apprentissage** 

À la fin de ce TP, vous devrez soumettre un dépôt Git fonctionnel contenant un code source modulaire, testé, capable de reproduire vos graphiques et métriques en "un clic", tout en garantissant la robustesse mathématique de vos algorithmes de résolution. 

## **2 Partie 1 : Isolation et Gestion de l’Environnement** 

Votre premier travail consiste à déclarer les dépendances logicielles de votre projet pour éviter le syndrome du _"Ça fonctionne sur ma machine"_ . 

**Travail À Faire (** � **)** 

Complétez le fichier `environment.yml` ci-dessous à la racine de votre dossier de projet. Vous devez configurer l’environnement pour utiliser Python 3.10 et intégrer les paquets scientifiques fondamentaux : `numpy` , `pandas` , `scikit-learn` , `matplotlib` et `pyyaml` . 

1 `# TODO: Completez les zones manquantes du fichier d’environnement` 2 `name: air_quality_stability` 3 `channels:` 4 `- conda -forge` 5 `- defaults` 6 `dependencies:` 7 `- python =3.10` 8 `# --- TODO: Ajoutez ici les librairies indispensables ---` 9 10 11 `- pip` 12 `- pip:` 13 `- logging -config` 

**Travail À Faire (** � **)** 

Exécutez dans votre terminal les commandes nécessaires pour instancier cet environnement à l’aide de `mamba` (ou `conda` ) et l’activer. Écrivez les commandes ci-dessous : 

1 _`# 1. Commande de creation de l’environnement a partir du fichier YAML :`_ 2 _`# TODO:`_ 3 4 _`# 2. Commande d’activation de l’environnement :`_ 

2 

5 _`# TODO:`_ 

## **3 Partie 2 : Configuration et Modularité du Pipeline** 

Pour qu’une expérience soit reproductible, les hyperparamètres (chemins de fichiers, graines aléatoires, configuration du modèle) ne doivent jamais être codés "en dur" dans les scripts Python. 

## **Travail À Faire (** � **)** 

Analysez et complétez les sections vides du script de prétraitement `src/preprocessing.py` . Vous devez implémenter l’imputation des valeurs manquantes par la **médiane** et réaliser la coupure des _outliers_ par la méthode de l’écart interquartile (IQR). 

1 `"""` 2 `Module de pretraitement des donnees - A C O M P L T E R` 3 `"""` 4 **`import`** `numpy` **`as`** `np` 5 **`import`** `pandas` **`as`** `pd` 6 **`from`** `sklearn. model_selection` **`import`** `train_test_split` 7 **`from`** `sklearn.preprocessing` **`import`** `StandardScaler` 8 **`from`** `typing` **`import`** `Tuple , Dict , Any` 9 10 **`def`** `clean_data(df: pd.DataFrame , config: Dict[` **`str`** `, Any]) -> pd. DataFrame:` 11 `df_clean = df.copy ()` 12 `handle_missing = config[ ’preprocessing ’ ][ ’handle_missing ’]` 13 14 _`# 1. Gestion des valeurs manquantes`_ 15 **`for`** `col` **`in`** `df_clean.columns:` 16 **`if`** `df_clean[col]. isnull ().` **`any`** `():` 17 **`if`** `handle_missing == ’median ’:` 18 _`# TODO: Calculez la mediane et imputez les valeurs manquantes`_ 19 **`pass`** 20 21 _`# 2. Gestion des valeurs aberrantes (Outliers via IQR)`_ 22 `threshold = config[ ’preprocessing ’ ][ ’outlier_threshold ’]` 23 `numeric_cols = df_clean.select_dtypes(include =[np.number ]). columns` 24 25 **`for`** `col` **`in`** `numeric_cols:` 26 **`if`** `col != config[ ’data ’ ][ ’target_column ’ ]:` 27 `Q1 = df_clean[col]. quantile (0.25)` 28 `Q3 = df_clean[col]. quantile (0.75)` 29 `IQR = Q3 - Q1` 30 _`# TODO: Definissez les bornes inferieure (lower) et superieure (upper)`_ 31 `lower =` _`# TODO`_ 32 `upper =` _`# TODO`_ 33 34 _`# Application du clipping pour neutraliser les outliers`_ 35 `df_clean[col] = df_clean[col]. clip(lower , upper)` 36 37 **`return`** `df_clean` 

3 

## **4 Partie 3 : Définition des Tests Unitaires** 

La validation de la reproductibilité logicielle passe par l’écriture de tests unitaires automatisés s’assurant du comportement invariant de vos blocs algorithmiques. 

**Travail À Faire (** � **)** 

Complétez le script de test `tests/test_preprocessing.py` en utilisant le framework `pytest` . Vous devez écrire un test qui vérifie que deux appels consécutifs à la fonction de partitionnement des données produisent **exactement** le même résultat lorsque la graine de reproductibilité ( _seed_ ) est fixée. 

1 **`import`** `numpy` **`as`** `np` 2 **`import`** `pandas` **`as`** `pd` 3 **`import`** `pytest` 4 **`from`** `src.preprocessing` **`import`** `split_and_scale` 5 6 **`def`** `test_split_reproducibility ():` 7 `""" V r i f i e l’invariance du partitionnement via la graine aleatoire."""` 8 _`# Generation d’un jeu de donnees de test fictif`_ 9 `df = pd.DataFrame ({` 10 `’feature1 ’:` **`range`** `(100) ,` 11 `’feature2 ’:` **`range`** `(100) ,` 12 `’pm25 ’:` **`range`** `(100)` 13 `})` 14 15 `config = {` 16 `’data ’: { ’target_column ’: ’pm25 ’ , ’test_size ’: 0.2},` 17 `’project ’: { ’random_seed ’: 42},` 18 `’preprocessing ’: { ’scale_features ’: True}` 19 `}` 20 21 _`# TODO: Realisez deux appels successifs a split_and_scale avec la meme config`_ 22 _`# et utilisez np.testing. assert_array_equal pour valider l’ identite des matrices.`_ 23 24 _`# X_train1 , X_test1 , _, _, _ = split_and_scale (df , config)`_ 25 _`# X_train2 , X_test2 , _, _, _ = split_and_scale (df , config)`_ 26 27 _`# TODO: Inserez vos assertions ici`_ 

## **5 Partie 4 : Gestion du Versionnement Scientifique** 

Un dépôt Git de recherche ne doit jamais stocker de gros volumes de données brutes, de fichiers de caches ou de checkpoints volumineux sous peine de saturer le serveur. 

4 

## **Travail À Faire (** � **)** 

Rédigez le contenu du fichier `.gitignore` à placer à la racine de votre projet afin d’exclure les fichiers de données brutes se trouvant dans le sous-dossier `data/` , tout en conservant l’architecture de vos répertoires à l’aide des fichiers de préservation `.gitkeep` . 

1 `# TODO: Ecrivez les regles d’exclusion de votre .gitignore scientifique` 2 `# Exclure les caches Python :` 3 4 5 `# Exclure le contenu des repertoires de donnees (conserver uniquement les fichiers d’architecture)` 6 7 8 `# Exclure les checkpoints Jupyter Notebooks :` 

## **6 Partie 5 : Intégration dans l’Orchestrateur Principal** 

Le script `src/main.py` est le chef d’orchestre unifié de votre projet de recherche. 

**Travail À Faire (** � **)** 

Complétez la fonction principale du fichier `src/main.py` pour y intégrer le chargement de la configuration externalisée YAML et s’assurer du basculement du moteur graphique Matplotlib en mode non-interactif (‘Agg‘) pour éviter les erreurs d’affichage en environnement serveur ou conteneurisé. 

1 **`import`** `sys` 2 **`import`** `os` 3 **`import`** `matplotlib` 4 _`# TODO: Basculez matplotlib sur le backend non -interactif ’Agg’`_ 5 _`# matplotlib.use (...)`_ 6 7 **`import`** `matplotlib.pyplot` **`as`** `plt` 8 **`from`** `src.utils` **`import`** `load_config , set_seed` 9 10 **`def`** `main ():` 11 _`# TODO: Chargez le fichier de configuration configs/config.yaml`_ 12 `config =` _`# TODO`_ 13 14 _`# Fixation universelle des graines pour la reproductibilite`_ 15 `set_seed(config[ ’project ’ ][ ’random_seed ’ ])` 16 17 **`print`** `( "[RUN] Execution du pipeline scientifique ..." )` 18 _`# Le reste du code execute les etapes de pretraitement et d’ apprentissage ...`_ 19 20 **`if`** `__name__ == "__main__" :` 21 `main ()` 

5 

## **7 Partie 6 : Stabilité Numérique** 

**Objectif d’Apprentissage** 

Ce TP d’application directe de notre dernier cours de cours magistral vise à vous faire toucher du doigt la fracture épistémologique entre la théorie mathématique en précision infinie (R) et la réalité physique de l’arithmétique des ordinateurs (Standard IEEE 754). 

## **7.1 Exercice 6.1 : Horizon de visibilité numérique et non-associativité** 

On cherche à observer l’accumulation des erreurs d’arrondis lors de la manipulation de nombres d’ordres de grandeur très différents. 

**Travail À Faire (** � **)** 

Complétez le script Python ci-dessous pour implémenter la méthode de sommation naïve d’un tableau de flottants et comparez-la à la **sommation de Kahan** vue en cours. Complétez les lignes de calcul manquantes. 

1 **`import`** `numpy` **`as`** `np` 2 3 **`def`** `sommation_naive (tableau):` 4 `somme = 0.0` 5 **`for`** `x` **`in`** `tableau:` 6 `somme += x` 7 **`return`** `somme` 8 9 **`def`** `sommation_kahan (tableau):` 10 `somme = 0.0` 11 `c = 0.0` _`# Une variable de compensation pour accumuler les petits arrondis perdus`_ 12 **`for`** `x` **`in`** `tableau:` 13 _`# TODO: Implementez l’algorithme de Kahan vu en cours`_ 14 `y = x - c` 15 `t = somme + y` 16 `c = (t - somme) - y` _`# Compensation de l’erreur d’arrondi`_ 17 `somme = t` 18 **`return`** `somme` 19 20 _`# --- Verification experimentale ---`_ 21 `epsilon_machine = np.finfo(` **`float`** `).eps` 22 **`print`** `(f "Epsilon Machine de votre processeur : { epsilon_machine }" )` 23 24 _`# On cree un tableau contenant un grand nombre suivi de beaucoup de petites valeurs`_ 25 `valeurs = [1.0] + [ epsilon_machine / 2.0] * 10000` 26 27 **`print`** `(f "Somme Naive : { sommation_naive (valeurs)}" )` 28 **`print`** `(f "Somme Kahan : { sommation_kahan (valeurs)}" )` 

6 

## **7.2 Exercice 6.2 : Le Multiplicateur de Mensonge — Sensibilité au conditionnement** 

On étudie la résolution du système linéaire linéaire _Ax_ = _b_ , où _A_ est une matrice mal conditionnée. On va analyser comment une infime perturbation de l’ordre de grandeur du bruit machine (10 _[−]_[14] ) sur le vecteur de mesures _b_ modifie radicalement le vecteur de prédiction _x_ . 

## **Travail À Faire (** � **)** 

Complétez le script de perturbation matricielle. Calculez le conditionnement de la matrice _A_ à l’aide de la fonction appropriée de NumPy, résolvez les deux systèmes (exact et perturbé), et calculez l’erreur relative induite sur la solution. 

1 **`import`** `numpy` **`as`** `np` 2 3 _`# Generation d’une matrice mal conditionnee (Ex: Matrice de Hilbert modifiee)`_ 4 `A = np.array ([[1.0 , 1.0/2.0 , 1.0/3.0] ,` 5 `[1.0/2.0 , 1.0/3.0 , 1.0/4.0] ,` 6 `[1.0/3.0 , 1.0/4.0 , 1.0/5.0]])` 7 8 `b = np.array ([1.0 , 0.5, 0.3333])` 9 10 _`# 1. Calcul du conditionnement`_ 11 _`# TODO: Utilisez la fonction numpy appropriee pour evaluer kappa(A) en norme 2`_ 12 `kappa_A =` _`# TODO`_ 13 14 **`print`** `(f " Conditionnement de la matrice de resolution kappa(A) = { kappa_A}" )` 15 16 _`# 2. Resolution du systeme exact`_ 17 _`# TODO: Utilisez le solveur lineaire stable de numpy (Bannir explicitement .inv())`_ 18 `x_exact =` _`# TODO`_ 19 20 _`# 3. Injection d’une perturbation microscopique sur le vecteur b`_ 21 `b_perturbe = b.copy ()` 22 `b_perturbe [2] += 1e-14` _`# Perturbation de l’ordre du bruit de mesure`_ 23 24 _`# Resolution du systeme avec perturbation`_ 25 `x_perturbe =` _`# TODO`_ 26 27 _`# 4. Calcul de l’erreur relative sur la solution finale`_ 28 `erreur_relative = np.linalg.norm(x_exact - x_perturbe) / np.linalg. norm(x_exact)` 29 **`print`** `(f "Erreur relative induite sur la prediction x : { erreur_relative :.6f}" )` 

## **7.3 Exercice 6.3 : Inversion Explicite vs Factorisation Directe** 

Dans cet exercice, nous allons quantifier expérimentalement pourquoi le cours recommande de **bannir l’utilisation de l’inversion explicite** d’une matrice. 

7 

## **Travail À Faire (** � **)** 

Complétez le code ci-dessous pour mesurer le temps d’exécution et la norme du résidu géométrique _∥Ax − b∥_ pour deux approches de résolution d’un système linéaire de grande taille ( _n_ = 4000) : l’approche par calcul explicite de l’inverse ( _A[−]_[1] _b_ ) et l’approche par factorisation directe (‘np.linalg.solve‘). 

1 **`import`** `numpy` **`as`** `np` 2 **`import`** `time` 3 4 _`# Generation d’un systeme lineaire dense de grande taille`_ 5 `n = 4000` 6 `rng = np.random.RandomState (42)` 7 `A = rng.rand(n, n)` 8 `b = rng.rand(n)` 9 10 _`# --- Approche A : Mauvaise pratique (Inversion explicite) ---`_ 11 `t0 = time.time ()` 12 _`# TODO: Calculez x en inversant explicitement la matrice A`_ 13 `x_inv =` _`# TODO`_ 14 `t_inv = time.time () - t0` 15 `residu_inv = np.linalg.norm(A @ x_inv - b)` 16 17 _`# --- Approche B : Bonne pratique (Factorisation stable) ---`_ 18 `t0 = time.time ()` 19 _`# TODO: Calculez x en utilisant le solveur direct stable de numpy`_ 20 `x_solve =` _`# TODO`_ 21 `t_solve = time.time () - t0` 22 `residu_solve = np.linalg.norm(A @ x_solve - b)` 23 24 **`print`** `(f "Inversion Explicite -> Temps: {t_inv :.4f}s | Norme du residu: {residu_inv}" )` 25 **`print`** `(f "Solveur Direct -> Temps: {t_solve :.4f}s | Norme du residu: {residu_solve}" )` 

## **8 Questions de Réflexion et Livrables** 

## **8.1 Questions de Contrôle de Validation (À rédiger sur votre rapport)** 

1. **Analyse Algorithmique :** Dans l’exercice 6.3, quel a été le ratio de gain de temps entre le solveur direct et l’inversion explicite ? Justifiez ce résultat à partir des complexités d’opérations en virgule flottante (Flops) vues en cours. 

2. **Interprétation Mathématique :** Si une matrice possède un conditionnement de _κ_ ( _A_ ) = 10[13] , et que votre machine travaille avec un epsilon machine de _ϵmach ≈_ 10 _[−]_[16] , combien de chiffres significatifs de votre vecteur solution _x_ sont mathématiquement exacts ? Que représentent les autres chiffres ? 

3. **Éthique et Rigueur :** Pourquoi la modification directe, manuelle et non-documentée d’un fichier de données brutes est-elle considérée comme une faute de méthode scientifique majeure ? 

8 

## **8.2 Livrables** 

Vous devez nous envoyer sur le groupe de D1 l’adresse URL de votre dépôt Git public contenant : 

- L’architecture complète des dossiers respectant le schéma cible. 

- Le fichier `environment.yml` complété et valide. 

- Les fichiers sources de la librairie `src/` exempts de tout bug. 

- Le fichier de configuration `configs/config.yaml` . 

- Un fichier `README.md` expliquant la commande unique permettant d’exécuter l’intégralité du pipeline et des tests. 

9 

